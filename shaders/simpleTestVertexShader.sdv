#version 330 core
layout (location = 0) in vec2 aPos;
layout (location = 1) in vec2 aTextCoord;

out vec2 textCoord;

uniform vec2 translation;

void main(){
gl_Position = vec4(aPos.x + translation.x, aPos.y + translation.y, 0.0, 1.0);
textCoord = aTextCoord;
}